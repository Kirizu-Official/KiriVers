import{Rt as e,dt as t,mt as n,n as r}from"./chunks/framework.BkIgthgQ.js";var i=JSON.parse(`{"title":"发版与 Docker","description":"手动触发的服务端发版、Conventional Commits 版本推导、十平台矩阵与 libc 策略、更新日志格式与自定义、Docker 多架构标签。","frontmatter":{"title":"发版与 Docker","description":"手动触发的服务端发版、Conventional Commits 版本推导、十平台矩阵与 libc 策略、更新日志格式与自定义、Docker 多架构标签。"},"headers":[],"relativePath":"contribute/release.md","filePath":"contribute/release.md","lastUpdated":1790057721000}`),a={name:`contribute/release.md`};function o(r,i,a,o,s,c){return e(),t(`div`,null,[...i[0]||=[n(`<h1 id="发版与-docker" tabindex="-1">发版与 Docker <a class="header-anchor" href="#发版与-docker" aria-label="Permalink to “发版与 Docker”">​</a></h1><p>维护者把进程镜像和官方二进制从默认分支发出去。使用者安装说明在 <a href="/guide/install/">指南 → 安装</a>。本页只写仓库里的流水线与本地 <code>dev/build</code>。发版工具链的唯一入口是 <code>dev/build/kirivers.py</code>：Linux / macOS 上 <code>python3 dev/build/kirivers.py &lt;子命令&gt;</code>，Windows 上 <code>python dev/build/kirivers.py &lt;子命令&gt;</code>（纯标准库，无需安装）。下文只写子命令名。</p><p>GitHub 是唯一的托管端（forge）：检查、发版、镜像都在 GitHub Actions，仓库里没有第二套流水线。</p><h2 id="路径" tabindex="-1">路径 <a class="header-anchor" href="#路径" aria-label="Permalink to “路径”">​</a></h2><table tabindex="0"><thead><tr><th>用途</th><th>路径</th></tr></thead><tbody><tr><td>运行镜像（Alpine，无数据库）</td><td><code>dev/build/Dockerfile</code>，构建上下文为仓库根</td></tr><tr><td>一键运行（Hub 镜像 + Postgres + Redis）</td><td><code>deploy/</code>；口令在 <code>deploy/.env</code>（使用者文档，见 <a href="/guide/install/compose">Docker Compose</a>）</td></tr><tr><td>发版 CLI</td><td><code>dev/build/kirivers.py</code> + 包 <code>dev/build/kirivers_build/</code>（纯标准库 Python；工作流只做编排，判定都在 CLI 里）</td></tr><tr><td>GitHub PR 检查</td><td><code>.github/workflows/ci.yml</code>（<code>pull_request</code> + <code>push</code> → <code>main</code>）</td></tr><tr><td>PR 守卫</td><td><code>.github/workflows/pr-guard.yml</code>（标题与 Issue 绑定，见 <a href="/contribute/pull-requests">贡献代码</a>）</td></tr><tr><td>安全门禁</td><td><code>.github/workflows/gosec-scan.yml</code>（<code>wait-merge</code> 标签触发只读沙箱 Gosec 扫描）+ <code>.github/workflows/security-gate.yml</code>（扫描通过后自动晋升 <code>ready-merge</code>；新提交即时撤销 <code>wait-merge</code> / <code>ready-merge</code>）</td></tr><tr><td>GitHub 发版</td><td><code>.github/workflows/release.yml</code>（<strong>只</strong> <code>workflow_dispatch</code>，不 <code>on: push</code>、不 <code>on: tags</code>）</td></tr><tr><td>更新日志类型表</td><td><code>dev/build/changelog-types.conf</code>（显示层，不参与版本推导）</td></tr><tr><td>发版追加的日志</td><td>仓库根 <code>CHANGELOG.md</code>（由 <code>dev/build/kirivers.py changelog</code> 写回 <code>main</code>）</td></tr><tr><td>官方 SDK 发版</td><td>各 <code>sdk/&lt;语言&gt;</code> 分支自带的 <code>.github/workflows/</code>；维护者也可用默认分支的 <code>sdk-automerge.yml</code>（<strong>只</strong> <code>workflow_dispatch</code>）批量合并 <code>ready-merge</code> PR 并在同一运行内推送各语言 registry，见 <code>docs/sdk-publish.md</code></td></tr></tbody></table><p>不要在仓库根添加 <code>compose.yml</code> / <code>docker-compose.yml</code>。镜像内不安装 <code>hdiffz</code> / <code>hpatchz</code>，官方 HDIFF13 由进程内 CGO 产出。</p><h2 id="触发-合入-main-不会发布" tabindex="-1">触发：合入 main 不会发布 <a class="header-anchor" href="#触发-合入-main-不会发布" aria-label="Permalink to “触发：合入 main 不会发布”">​</a></h2><p>服务端发版是<strong>手动独占</strong>的：把 PR 合进 <code>main</code> 只跑检查。要发版，去 Actions → Release → <strong>Run workflow</strong>。</p><ul><li>版本号自动推导，<strong>没有</strong>手填版本的入口，也没有&quot;强制升一级&quot;的开关。</li><li>区间内没有可发版提交（只有 <code>chore:</code> / <code>docs:</code> / <code>ci:</code> 等）时，本次运行<strong>绿色结束</strong>，不打 tag、不建 Release、不推镜像，并在 job summary 里写明原因。</li><li><code>SERVER_PUBLISH</code> 是手动 kill-switch：设为 <code>false</code> 时即使有可发版提交也只推导版本，不产出任何外部副作用。</li></ul><h2 id="版本-与-sdk-同构-tag-前缀不同" tabindex="-1">版本（与 SDK 同构，tag 前缀不同） <a class="header-anchor" href="#版本-与-sdk-同构-tag-前缀不同" aria-label="Permalink to “版本（与 SDK 同构，tag 前缀不同）”">​</a></h2><p>服务端 tag 是打在 <code>main</code> 祖先上的 annotated <code>v&lt;semver&gt;</code>（例如 <code>v0.1.0</code>）。查找上一版本时忽略 <code>sdk-*-v*</code>。</p><ol><li>没有服务端 <code>v*</code> → 发布基线 <strong><code>0.1.0</code></strong>（即使该提交是 <code>ci:</code> / <code>chore:</code>），不回放历史 <code>feat</code>。</li><li>已有 <code>v*</code> → <code>git log &lt;prev&gt;..HEAD</code>（去掉 merge）：<code>feat</code> → minor；<code>fix</code> / <code>perf</code> / <code>revert</code> → patch；正文 <code>BREAKING CHANGE:</code> 或 <code>type!:</code> → major。<code>chore</code> / <code>docs</code> / <code>ci</code> / <code>test</code> / <code>style</code> / <code>refactor</code> <strong>不发版</strong>。</li><li>breaking 从 <code>0.1.x</code> 跳到 <code>1.0.0</code>。</li><li>纯 <code>docs:</code> 合入 <code>main</code>：检查通过，且手动运行时不发版、不打新 tag、不推 Hub。</li><li>仓库没有服务端 VERSION 文件，不提交 <code>chore(release)</code>。</li></ol><p>实现：<code>next-version</code> 子命令（<code>dev/build/kirivers_build/version.py</code>）。PR 用 <code>commitlint</code> 校验标题与提交；直推 <code>main</code> 时改测 <code>before..after</code> 区间（<code>before</code> 全零或不可达时退化为只检最新提交）。不合规的 squash 标题会使检查失败。</p><h2 id="平台矩阵-十种组合-零-qemu" tabindex="-1">平台矩阵：十种组合，零 QEMU <a class="header-anchor" href="#平台矩阵-十种组合-零-qemu" aria-label="Permalink to “平台矩阵：十种组合，零 QEMU”">​</a></h2><p>一律 <code>yarn build</code> 之后 <strong><code>CGO_ENABLED=1</code></strong>。<code>CGO_ENABLED=0</code> 不是发版路径（CI 会断言它编译失败）。</p><table tabindex="0"><thead><tr><th>目标</th><th>libc</th><th>宿主</th><th>构建方式</th></tr></thead><tbody><tr><td>linux/amd64</td><td><strong>musl</strong></td><td><code>ubuntu-latest</code></td><td>Alpine 容器内原生 gcc/g++</td></tr><tr><td>linux/arm64</td><td><strong>musl</strong></td><td><code>ubuntu-24.04-arm</code></td><td>同上，arm64 原生宿主</td></tr><tr><td>linux/386</td><td>glibc</td><td><code>ubuntu-latest</code></td><td>amd64 宿主交叉：<code>g++-i686-linux-gnu</code></td></tr><tr><td>linux/arm (armv7)</td><td>glibc</td><td><code>ubuntu-latest</code></td><td>amd64 宿主交叉：<code>g++-arm-linux-gnueabihf</code></td></tr><tr><td>linux/riscv64</td><td>glibc</td><td><code>ubuntu-latest</code></td><td>amd64 宿主交叉：<code>g++-riscv64-linux-gnu</code></td></tr><tr><td>darwin/amd64</td><td>—</td><td><code>macos-15</code></td><td>Apple clang <code>-arch x86_64</code></td></tr><tr><td>darwin/arm64</td><td>—</td><td><code>macos-15</code></td><td>Apple clang 原生</td></tr><tr><td>windows/386</td><td>—</td><td><code>windows-latest</code></td><td>llvm-mingw <code>i686-w64-mingw32</code></td></tr><tr><td>windows/amd64</td><td>—</td><td><code>windows-latest</code></td><td>llvm-mingw <code>x86_64-w64-mingw32</code></td></tr><tr><td>windows/arm64</td><td>—</td><td><code>windows-latest</code></td><td>llvm-mingw <code>aarch64-w64-mingw32</code></td></tr></tbody></table><ul><li><strong>全程不使用 QEMU 用户态模拟</strong>：musl 只在能原生产出的架构上用，其余 Linux 架构退化为 glibc 交叉编译。代价写进<a href="/guide/install/manual">手动安装</a>：glibc 产物要求目标机 glibc ≥ 2.39，armv7 需 hard-float。</li><li>交叉编译器静默回落宿主是最危险的失败模式，因此 <code>build-cgo</code> 每个目标编完用 <code>file</code> 断言机器类型，打包时在 Linux 作业里对十个产物再断言一次。</li><li>darwin 产物只在 macOS 宿主上产出，<strong>绝不</strong>从 Linux 伪造。</li><li>编译作业 <code>fail-fast: false</code>；但缺件时 <code>package</code> 与 <code>github-release</code> 必须失败，宁可不发也不发半个版本。</li></ul><h2 id="产物-归档名带内容哈希" tabindex="-1">产物：归档名带内容哈希 <a class="header-anchor" href="#产物-归档名带内容哈希" aria-label="Permalink to “产物：归档名带内容哈希”">​</a></h2><p>内部二进制名保持稳定（<code>kirivers-&lt;os&gt;-&lt;arch&gt;[.exe]</code>），上传的是每目标一个 zip：</p><div class="language- line-numbers-mode"><button title="Copy code" data-copied="Copied" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span>KiriVers-&lt;OS&gt;-&lt;Arch&gt;-&lt;sha6&gt;.zip</span></span>
<span class="line"><span>KiriVers-Linux-x86_64-449c6d.zip     KiriVers-macOS-arm64-ad1a24.zip</span></span>
<span class="line"><span>KiriVers-Linux-x86-108619.zip        KiriVers-Windows-x86-635890.zip</span></span>
<span class="line"><span>KiriVers-Linux-armv7-ec6a8c.zip      KiriVers-Windows-x86_64-abb5fc.zip</span></span>
<span class="line"><span>KiriVers-Linux-arm64-0c690f.zip      KiriVers-Windows-arm64-0f98c4.zip</span></span>
<span class="line"><span>KiriVers-Linux-riscv64-074587.zip</span></span>
<span class="line"><span>frontend-dist.zip                    SHA256SUMS.txt</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br></div></div><p><code>&lt;sha6&gt;</code> = 包内二进制 SHA-256 的前 6 位小写十六进制（文件无法自哈希，所以文件名每次发版都会变）。OS token 用 <code>Linux</code> / <code>macOS</code> / <code>Windows</code>，Arch token 用 <code>x86</code> / <code>x86_64</code> / <code>armv7</code> / <code>arm64</code> / <code>riscv64</code>。映射表只定义在 <code>dev/build/kirivers_build/common.py</code>，<code>asset-names</code> 与本页表格都由它派生。</p><p><code>SHA256SUMS.txt</code> 两段式：第一段是 11 个上传件自身（可直接 <code>sha256sum -c SHA256SUMS.txt</code>），第二段以注释记录 10 个包内二进制的哈希（<code>#</code> 前缀保证不被 <code>-c</code> 解析），解压后可自行比对。</p><p><code>frontend-dist.zip</code> 给&quot;只想换管理台静态文件&quot;的场景：包内是 <code>dist/</code> 的内容，根目录就是 <code>index.html</code>。官方二进制已内嵌管理台，使用者不需要它。</p><h2 id="changelog-format" tabindex="-1">更新日志：按 type 分节，scope 做前缀 <a class="header-anchor" href="#changelog-format" aria-label="Permalink to “更新日志：按 type 分节，scope 做前缀”">​</a></h2><p>Release 正文与仓库根 <code>CHANGELOG.md</code> 由同一个 <code>release-notes</code> 子命令（<code>dev/build/kirivers_build/notes.py</code>）输出生成，格式与 SDK 分支的 <code>scripts/sdk_release.py</code> 一致：</p><div class="language-markdown line-numbers-mode"><button title="Copy code" data-copied="Copied" class="copy"></button><span class="lang">markdown</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#005CC5;--shiki-light-font-weight:bold;--shiki-dark:#79B8FF;--shiki-dark-font-weight:bold;">## v0.3.0 — 2026-10-05</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-light-font-weight:bold;--shiki-dark:#79B8FF;--shiki-dark-font-weight:bold;">### 💥 Breaking Changes</span></span>
<span class="line"><span style="--shiki-light:#c13617;--shiki-dark:#FFAB70;">-</span><span style="--shiki-light:#24292E;--shiki-light-font-weight:bold;--shiki-dark:#E1E4E8;--shiki-dark-font-weight:bold;"> **admin**</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> feat: 重写 API Token 轮换，旧接口下线 (8a9b0c1) #31</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-light-font-weight:bold;--shiki-dark:#79B8FF;--shiki-dark-font-weight:bold;">### ✨ Features</span></span>
<span class="line"><span style="--shiki-light:#c13617;--shiki-dark:#FFAB70;">-</span><span style="--shiki-light:#24292E;--shiki-light-font-weight:bold;--shiki-dark:#E1E4E8;--shiki-dark-font-weight:bold;"> **delta**</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: 支持流式源文件 (4f5e6d7) #28</span></span>
<span class="line"><span style="--shiki-light:#c13617;--shiki-dark:#FFAB70;">-</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> 直推且没有 scope 的 feat 照进本节 (6bc7e20)</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-light-font-weight:bold;--shiki-dark:#79B8FF;--shiki-dark-font-weight:bold;">### 🐛 Bug Fixes</span></span>
<span class="line"><span style="--shiki-light:#c13617;--shiki-dark:#FFAB70;">-</span><span style="--shiki-light:#24292E;--shiki-light-font-weight:bold;--shiki-dark:#E1E4E8;--shiki-dark-font-weight:bold;"> **delta**</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: 拒绝截断的 HDIFF13 头部 (a1b2c3d) #29</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br><span class="line-number">9</span><br><span class="line-number">10</span><br><span class="line-number">11</span><br></div></div><ul><li>章节 = commit 的 <code>type</code>；标题 = <code>### &lt;emoji&gt; &lt;文案&gt;</code>，emoji、文案与<strong>章节顺序</strong>都取自类型表（文件里的书写顺序即章节顺序）。</li><li>条目 = <code>- **&lt;scope&gt;**: &lt;说明&gt; (&lt;sha7&gt;) #&lt;PR&gt;</code>。<code>scope</code> 统一转小写并去空白（<code>perf(DB)</code> 与 <code>perf(db)</code> 同一个标注）；没有 scope 时省略 <code>**scope**:</code> 前缀，不塞进任何兜底章节。</li><li>破坏性条目（<code>type!:</code> 或正文 <code>BREAKING CHANGE:</code> / <code>BREAKING-CHANGE:</code>）离开自己的 type 节，集中到首节 <code>### 💥</code>，并在行内补回 type 词（因为章节标题已不携带它）。</li><li>PR 号是<strong>装饰</strong>：作业用 <code>gh api .../commits/&lt;sha&gt;/pulls</code> 查询，查不到、<code>gh</code> 缺失或离线时静默省略，绝不因此中断发版；说明末尾已有 <code>(#123)</code> 时不重复追加。</li><li>非 Conventional 格式的 subject 不进日志（与&quot;不发版&quot;档位一致）；未登记的合法 type 落 <code>### 🔖 Other</code>，<strong>不会静默丢条目</strong>；区间内一条可显示条目都没有时输出 <code>No user-facing Conventional Commits in this range.</code>。</li><li>没有前序 <code>v*</code> tag 时只写基线声明，不回放历史，不分章节。</li></ul><h3 id="自定义-emoji-文案-隐藏某类" tabindex="-1">自定义 emoji / 文案 / 隐藏某类 <a class="header-anchor" href="#自定义-emoji-文案-隐藏某类" aria-label="Permalink to “自定义 emoji / 文案 / 隐藏某类”">​</a></h3><p>唯一入口是 <code>dev/build/changelog-types.conf</code>（SDK 分支为 <code>scripts/changelog-types.conf</code>，内容相同）。下面是节选，真实文件还带一段说明注释以及 <code>test</code> / <code>style</code> / <code>ci</code> / <code>build</code> 等默认注释掉的行：</p><div class="language-ini line-numbers-mode"><button title="Copy code" data-copied="Copied" class="copy"></button><span class="lang">ini</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">breaking</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">=💥=Breaking Changes</span></span>
<span class="line"><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">feat</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">=✨=Features</span></span>
<span class="line"><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">fix</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">=🐛=Bug Fixes</span></span>
<span class="line"><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">perf</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">=⚡=Performance</span></span>
<span class="line"><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">revert</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">=⏪=Reverts</span></span>
<span class="line"><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">refactor</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">=♻️=Refactoring</span></span>
<span class="line"><span style="--shiki-light:#62687b;--shiki-dark:#818e99;"># docs=📝=Documentation</span></span>
<span class="line"><span style="--shiki-light:#62687b;--shiki-dark:#818e99;"># chore=🔧=Chores</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br></div></div><p>一行一项 <code>type=emoji[=章节文案]</code>（<code>type=emoji 文案</code> 也接受）；整行以 <code>#</code> 开头且形如 <code># type=…</code> 表示<strong>该类不进更新日志</strong>，其余 <code>#</code> 行是说明文字。格式非法（键不是小写字母开头、缺 emoji、同一个 type 写两遍）直接报错退出，不静默忽略。文件缺失时脚本回落到内置默认表，并断言&quot;解析结果与文件一致&quot;，防止两份默认漂移。</p><div class="danger custom-block"><p class="custom-block-title">这条不变量请守住</p><p>类型表<strong>只是显示层</strong>。<code>next-version</code> 的档位表与 <code>sdk_release.py</code> 的 <code>RELEASE_TYPES</code> 绝不读它——注释掉 <code>fix=</code> 只会让 Bug Fixes 章节消失，patch 升档照旧。回归断言在 <code>release-notes --self-check</code>：改配置前后 <code>next-version --format env</code> 的输出必须逐字节相同。</p></div><h2 id="changelog-md-写回-main" tabindex="-1">CHANGELOG.md 写回 main <a class="header-anchor" href="#changelog-md-写回-main" aria-label="Permalink to “CHANGELOG.md 写回 main”">​</a></h2><p><code>github-release</code> 作业在建 tag、发 Release <strong>之后</strong>追加 <code>CHANGELOG.md</code> 头部并 <code>git push origin HEAD:main</code>，提交信息 <code>docs(release): v&lt;semver&gt; changelog</code>（<code>docs:</code> 不计入下一次发版，不会自造版本差）。因此该提交不属于被发布的 commit 集合，也不在 <code>&lt;semver&gt;</code> tag 的祖先里。</p><p>这要求仓库允许 <code>github-actions[bot]</code> 用 <code>GITHUB_TOKEN</code> 写 <code>main</code>（与 SDK 分支放行 bot 合并同构）。若分支保护没放开，该步骤<strong>失败并说明原因</strong>，不会静默跳过。</p><h2 id="docker-每架构原生构建-远端合成-manifest" tabindex="-1">Docker：每架构原生构建 + 远端合成 manifest <a class="header-anchor" href="#docker-每架构原生构建-远端合成-manifest" aria-label="Permalink to “Docker：每架构原生构建 + 远端合成 manifest”">​</a></h2><table tabindex="0"><thead><tr><th>标签</th><th>含义</th></tr></thead><tbody><tr><td><code>kirizuofficial/kirivers:latest</code></td><td>指向本次发版</td></tr><tr><td><code>kirizuofficial/kirivers:&lt;semver&gt;</code></td><td>本次发版的多架构 manifest</td></tr><tr><td><code>kirizuofficial/kirivers:&lt;semver&gt;_amd64</code></td><td>该版本的 amd64 单架构镜像</td></tr><tr><td><code>kirizuofficial/kirivers:&lt;semver&gt;_arm64</code></td><td>该版本的 arm64 单架构镜像</td></tr></tbody></table><p>两个架构各在<strong>原生 runner</strong> 上用 <code>docker/login-action@v3</code> + <code>docker/build-push-action@v6</code>（<code>--build-arg USE_PREBUILT=1</code> 复用已编好的 musl 二进制）推每架构标签；再由 <code>docker-manifest</code> 作业用 <code>buildx imagetools create</code> 在注册表侧合成 <code>:latest</code> 与 <code>:&lt;semver&gt;</code>。<code>imagetools create</code> 不执行镜像内任何命令，因此不需要 QEMU/binfmt。glibc 产物<strong>绝不</strong>进 Alpine 镜像：每架构作业只 COPY <code>linux-musl</code> 矩阵（musl 断言通过）的产物；本地手工路径 <code>docker-image</code> 同样只接受 musl 断言通过的二进制，且要求宿主架构与目标一致。</p><p>作业顺序保证只有两个每架构标签都推送成功才合成 manifest——合成错误标签会污染 <code>:latest</code>。</p><h2 id="密钥与关闭-publish" tabindex="-1">密钥与关闭 publish <a class="header-anchor" href="#密钥与关闭-publish" aria-label="Permalink to “密钥与关闭 publish”">​</a></h2><p>文档只列名称，不写值：</p><table tabindex="0"><thead><tr><th>名称</th><th>用途</th></tr></thead><tbody><tr><td><code>DOCKERHUB_USERNAME</code></td><td>Docker Hub 用户</td></tr><tr><td><code>DOCKERHUB_TOKEN</code></td><td>Docker Hub 登录</td></tr><tr><td><code>SERVER_PUBLISH</code></td><td>GitHub repository variable；<code>false</code> 时本次运行只推导版本</td></tr><tr><td><code>GITHUB_TOKEN</code></td><td>工作流自带；写 tag、Release、<code>CHANGELOG.md</code>，读 PR 号</td></tr></tbody></table><p>缺 Hub 凭据时<strong>推镜像步骤</strong>失败，并说明缺 <code>DOCKERHUB_USERNAME</code> 和/或 <code>DOCKERHUB_TOKEN</code>；日志不得打印 token。并发组名：<code>server-release</code>。</p><h2 id="本地构建镜像" tabindex="-1">本地构建镜像 <a class="header-anchor" href="#本地构建镜像" aria-label="Permalink to “本地构建镜像”">​</a></h2><div class="language-bash line-numbers-mode"><button title="Copy code" data-copied="Copied" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">docker</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> build</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --platform</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> linux/amd64</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -t</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> kirivers:linux-amd64</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -f</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> dev/build/Dockerfile</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> .</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>日常运行请用 Hub 镜像：使用者一键目录 <code>deploy/</code>（<code>image: kirizuofficial/kirivers:latest</code>，见 <a href="/guide/install/compose">Docker Compose</a>）。本机 <code>docker build</code> 只用于改 Dockerfile 之后再推 Hub。镜像默认 <code>KIRIVERS_STORAGE_LOCAL_ROOT=/data/storage</code>，日志目录 <code>/data/logs</code>。把宿主机目录挂到 <code>/config</code> 与 <code>/data</code> 即可持久化配置、全量/差量对象和日志。</p><p>HEALTHCHECK 访问管理平面 <code>GET /api/v1/health</code>（无库仍 HTTP 200）。容器以 root 运行以便 bind mount。</p><h2 id="必测项" tabindex="-1">必测项 <a class="header-anchor" href="#必测项" aria-label="Permalink to “必测项”">​</a></h2><p>改动这条链路后至少跑：</p><div class="language-bash line-numbers-mode"><button title="Copy code" data-copied="Copied" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">python3</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> dev/build/kirivers.py</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> check-workflows</span><span style="--shiki-light:#62687b;--shiki-dark:#818e99;">        # 静态门：触发方式、矩阵、无 QEMU、无其它 forge 残留</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">python3</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> dev/build/kirivers.py</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> release-notes</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --self-check</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">python3</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> dev/build/kirivers.py</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> guard</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --self-check</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">python3</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> dev/build/kirivers.py</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> issue-link</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --self-check</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">CGO_ENABLED</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">1</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> go</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> build</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> .</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> &amp;&amp; CGO_ENABLED</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">0</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> go</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> build</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> .</span><span style="--shiki-light:#62687b;--shiki-dark:#818e99;">   # 后者必须失败</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br></div></div><p>Windows 上把 <code>python3</code> 换成 <code>python</code>。<code>check-workflows</code> 已经把&quot;只有 <code>workflow_dispatch</code>&quot;&quot;没有 <code>setup-qemu-action</code>&quot;&quot;<code>pr-guard.yml</code> 不检出 PR 代码&quot;等写成断言，CI 的 <code>changelog</code> 作业会跑它。</p>`,51)]])}var s=r(a,[[`render`,o]]);export{i as __pageData,s as default};