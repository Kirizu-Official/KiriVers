import{Rt as e,dt as t,mt as n,n as r}from"./chunks/framework.BkIgthgQ.js";var i=JSON.parse(`{"title":"Release and Docker","description":"Manually triggered server release, Conventional Commits version derivation, the ten-target matrix and libc policy, changelog format and customisation, multi-arch Docker tags.","frontmatter":{"title":"Release and Docker","description":"Manually triggered server release, Conventional Commits version derivation, the ten-target matrix and libc policy, changelog format and customisation, multi-arch Docker tags."},"headers":[],"relativePath":"en/contribute/release.md","filePath":"en/contribute/release.md","lastUpdated":1790057721000}`),a={name:`en/contribute/release.md`};function o(r,i,a,o,s,c){return e(),t(`div`,null,[...i[0]||=[n(`<h1 id="release-and-docker" tabindex="-1">Release and Docker <a class="header-anchor" href="#release-and-docker" aria-label="Permalink to “Release and Docker”">​</a></h1><p>Maintainers publish the process image and the official binaries from the default branch. Operator install docs live under <a href="/en/guide/install/">Guide → Install</a>. This page covers the in-repo pipelines and the local <code>dev/build</code> toolchain only. The release toolchain has a single entry point, <code>dev/build/kirivers.py</code>: <code>python3 dev/build/kirivers.py &lt;command&gt;</code> on Linux / macOS and <code>python dev/build/kirivers.py &lt;command&gt;</code> on Windows (standard library only, nothing to install). The rest of this page names just the subcommand.</p><p>GitHub is the only forge: checks, releases and images all run in GitHub Actions, and the repo holds no second pipeline.</p><h2 id="paths" tabindex="-1">Paths <a class="header-anchor" href="#paths" aria-label="Permalink to “Paths”">​</a></h2><table tabindex="0"><thead><tr><th>Purpose</th><th>Path</th></tr></thead><tbody><tr><td>Runtime image (Alpine, no database)</td><td><code>dev/build/Dockerfile</code>, build context = repo root</td></tr><tr><td>One-file run (Hub image + Postgres + Redis)</td><td><code>deploy/</code>; passwords in <code>deploy/.env</code> (operator docs, see <a href="/en/guide/install/compose">Docker Compose</a>)</td></tr><tr><td>Release CLI</td><td><code>dev/build/kirivers.py</code> + package <code>dev/build/kirivers_build/</code> (standard-library Python; workflows only orchestrate, every decision lives in the CLI)</td></tr><tr><td>GitHub PR checks</td><td><code>.github/workflows/ci.yml</code> (<code>pull_request</code> + <code>push</code> → <code>main</code>)</td></tr><tr><td>PR guard</td><td><code>.github/workflows/pr-guard.yml</code> (title and issue link, see <a href="/en/contribute/pull-requests">Pull requests</a>)</td></tr><tr><td>Security gate</td><td><code>.github/workflows/gosec-scan.yml</code> (the <code>wait-merge</code> label starts a sandboxed Gosec scan) + <code>.github/workflows/security-gate.yml</code> (a clean scan auto-promotes to <code>ready-merge</code>; new commits instantly revoke <code>wait-merge</code> / <code>ready-merge</code>)</td></tr><tr><td>GitHub release</td><td><code>.github/workflows/release.yml</code> (<strong>only</strong> <code>workflow_dispatch</code>; never <code>on: push</code>, never <code>on: tags</code>)</td></tr><tr><td>Changelog type table</td><td><code>dev/build/changelog-types.conf</code> (display layer; not part of version derivation)</td></tr><tr><td>Changelog appended by releases</td><td>repo-root <code>CHANGELOG.md</code> (written back to <code>main</code> by <code>dev/build/kirivers.py changelog</code>)</td></tr><tr><td>Official SDK releases</td><td>the <code>.github/workflows/</code> each <code>sdk/&lt;lang&gt;</code> branch carries; maintainers can also run <code>sdk-automerge.yml</code> from the default branch (<strong>only</strong> <code>workflow_dispatch</code>) to batch-merge <code>ready-merge</code> PRs and push to each language registry in the same run, see <code>docs/sdk-publish.md</code></td></tr></tbody></table><p>Do not add <code>compose.yml</code> / <code>docker-compose.yml</code> at the repo root. The image installs no <code>hdiffz</code> / <code>hpatchz</code>; official HDIFF13 is produced in-process via CGO.</p><h2 id="trigger-merging-to-main-does-not-publish" tabindex="-1">Trigger: merging to main does not publish <a class="header-anchor" href="#trigger-merging-to-main-does-not-publish" aria-label="Permalink to “Trigger: merging to main does not publish”">​</a></h2><p>Server release is <strong>manual-only</strong>: merging a PR into <code>main</code> runs checks. To release, open Actions → Release → <strong>Run workflow</strong>.</p><ul><li>The version is derived automatically. There is <strong>no</strong> field to type a version and no &quot;force one level up&quot; switch.</li><li>When the range holds no releasable commits (only <code>chore:</code> / <code>docs:</code> / <code>ci:</code> and friends), the run <strong>finishes green</strong>: no tag, no Release, no image push, and the job summary states the reason.</li><li><code>SERVER_PUBLISH</code> is the manual kill-switch: set to <code>false</code>, the run derives the version even when there are releasable commits and produces no external side effect at all.</li></ul><h2 id="versioning-same-rules-as-the-sdks-different-tag-prefix" tabindex="-1">Versioning (same rules as the SDKs, different tag prefix) <a class="header-anchor" href="#versioning-same-rules-as-the-sdks-different-tag-prefix" aria-label="Permalink to “Versioning (same rules as the SDKs, different tag prefix)”">​</a></h2><p>Server tags are annotated <code>v&lt;semver&gt;</code> on an ancestor of <code>main</code> (for example <code>v0.1.0</code>). The previous-version lookup ignores <code>sdk-*-v*</code>.</p><ol><li>No server <code>v*</code> → publish the baseline <strong><code>0.1.0</code></strong> (even when the tip commit is <code>ci:</code> / <code>chore:</code>). Historical <code>feat</code> commits are not replayed.</li><li>A previous <code>v*</code> exists → <code>git log &lt;prev&gt;..HEAD</code> (no merges): <code>feat</code> → minor; <code>fix</code> / <code>perf</code> / <code>revert</code> → patch; body <code>BREAKING CHANGE:</code> or <code>type!:</code> → major. <code>chore</code> / <code>docs</code> / <code>ci</code> / <code>test</code> / <code>style</code> / <code>refactor</code> <strong>do not</strong> release.</li><li>A breaking change from <code>0.1.x</code> jumps to <code>1.0.0</code>.</li><li>A pure <code>docs:</code> merge into <code>main</code>: checks pass, and a manual run releases nothing — no new tag, no Hub push.</li><li>There is no server VERSION file, and the server never commits <code>chore(release)</code>.</li></ol><p>Implementation: the <code>next-version</code> subcommand (<code>dev/build/kirivers_build/version.py</code>). PRs are validated by <code>commitlint</code> against the title and the commits; on a direct push to <code>main</code> it degrades to the <code>before..after</code> range (checking only the tip commit when <code>before</code> is all zeros or unreachable). A non-conforming squash title fails the check.</p><h2 id="platform-matrix-ten-targets-zero-qemu" tabindex="-1">Platform matrix: ten targets, zero QEMU <a class="header-anchor" href="#platform-matrix-ten-targets-zero-qemu" aria-label="Permalink to “Platform matrix: ten targets, zero QEMU”">​</a></h2><p>Always <code>yarn build</code> first, then <strong><code>CGO_ENABLED=1</code></strong>. <code>CGO_ENABLED=0</code> is not a release path (CI asserts that it fails to compile).</p><table tabindex="0"><thead><tr><th>Target</th><th>libc</th><th>Host</th><th>How it builds</th></tr></thead><tbody><tr><td>linux/amd64</td><td><strong>musl</strong></td><td><code>ubuntu-latest</code></td><td>native gcc/g++ inside an Alpine container</td></tr><tr><td>linux/arm64</td><td><strong>musl</strong></td><td><code>ubuntu-24.04-arm</code></td><td>same, on a native arm64 host</td></tr><tr><td>linux/386</td><td>glibc</td><td><code>ubuntu-latest</code></td><td>cross from amd64: <code>g++-i686-linux-gnu</code></td></tr><tr><td>linux/arm (armv7)</td><td>glibc</td><td><code>ubuntu-latest</code></td><td>cross from amd64: <code>g++-arm-linux-gnueabihf</code></td></tr><tr><td>linux/riscv64</td><td>glibc</td><td><code>ubuntu-latest</code></td><td>cross from amd64: <code>g++-riscv64-linux-gnu</code></td></tr><tr><td>darwin/amd64</td><td>—</td><td><code>macos-15</code></td><td>Apple clang <code>-arch x86_64</code></td></tr><tr><td>darwin/arm64</td><td>—</td><td><code>macos-15</code></td><td>native Apple clang</td></tr><tr><td>windows/386</td><td>—</td><td><code>windows-latest</code></td><td>llvm-mingw <code>i686-w64-mingw32</code></td></tr><tr><td>windows/amd64</td><td>—</td><td><code>windows-latest</code></td><td>llvm-mingw <code>x86_64-w64-mingw32</code></td></tr><tr><td>windows/arm64</td><td>—</td><td><code>windows-latest</code></td><td>llvm-mingw <code>aarch64-w64-mingw32</code></td></tr></tbody></table><ul><li><strong>No QEMU user-mode emulation anywhere</strong>: musl is used only on architectures that can build it natively; the remaining Linux targets fall back to glibc cross builds. The cost is stated on <a href="/en/guide/install/manual">manual install</a>: glibc artifacts require glibc ≥ 2.39 on the target machine and armv7 requires hard-float.</li><li>A cross compiler silently falling back to the host binary is the most dangerous failure mode, so <code>build-cgo</code> asserts the machine type with <code>file</code> after every target, and packaging asserts again over all ten artifacts inside the Linux job.</li><li>darwin artifacts are produced only on a macOS host, <strong>never</strong> fabricated from Linux.</li><li>Build jobs run <code>fail-fast: false</code>; but <code>package</code> and <code>github-release</code> must fail when an artifact is missing. No release beats half a release.</li></ul><h2 id="artifacts-archive-names-carry-the-content-hash" tabindex="-1">Artifacts: archive names carry the content hash <a class="header-anchor" href="#artifacts-archive-names-carry-the-content-hash" aria-label="Permalink to “Artifacts: archive names carry the content hash”">​</a></h2><p>Internal binary names stay stable (<code>kirivers-&lt;os&gt;-&lt;arch&gt;[.exe]</code>); what is uploaded is one zip per target:</p><div class="language- line-numbers-mode"><button title="Copy code" data-copied="Copied" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span>KiriVers-&lt;OS&gt;-&lt;Arch&gt;-&lt;sha6&gt;.zip</span></span>
<span class="line"><span>KiriVers-Linux-x86_64-449c6d.zip     KiriVers-macOS-arm64-ad1a24.zip</span></span>
<span class="line"><span>KiriVers-Linux-x86-108619.zip        KiriVers-Windows-x86-635890.zip</span></span>
<span class="line"><span>KiriVers-Linux-armv7-ec6a8c.zip      KiriVers-Windows-x86_64-abb5fc.zip</span></span>
<span class="line"><span>KiriVers-Linux-arm64-0c690f.zip      KiriVers-Windows-arm64-0f98c4.zip</span></span>
<span class="line"><span>KiriVers-Linux-riscv64-074587.zip</span></span>
<span class="line"><span>frontend-dist.zip                    SHA256SUMS.txt</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br></div></div><p><code>&lt;sha6&gt;</code> is the first 6 lowercase hex characters of the SHA-256 of the binary inside the archive (a file cannot hash itself, so the name changes on every release) — recognise the archives by prefix on the Release page. OS tokens are <code>Linux</code> / <code>macOS</code> / <code>Windows</code>; Arch tokens are <code>x86</code> / <code>x86_64</code> / <code>armv7</code> / <code>arm64</code> / <code>riscv64</code> (darwin uses <code>x86_64</code> / <code>arm64</code>). The mapping is defined only in <code>dev/build/kirivers_build/common.py</code> (<code>MATRIX</code> / <code>archive_name</code>); <code>asset-names</code> and the table above derive from it.</p><p><code>SHA256SUMS.txt</code> has two sections: the first lists the 11 uploaded files and is directly verifiable with <code>sha256sum -c SHA256SUMS.txt</code>; the second records the hashes of the 10 in-archive binaries as <code>#</code> comments (the prefix keeps <code>-c</code> from parsing them), so you can compare them after extracting.</p><p><code>frontend-dist.zip</code> serves the &quot;I only want to swap the admin console files&quot; case: it holds the contents of <code>dist/</code>, so <code>index.html</code> sits at the root of the archive. Official binaries already embed the console; operators do not need this file.</p><h2 id="changelog-format" tabindex="-1">Changelog: one section per type, scope as the prefix <a class="header-anchor" href="#changelog-format" aria-label="Permalink to “Changelog: one section per type, scope as the prefix”">​</a></h2><p>The Release body and the repo-root <code>CHANGELOG.md</code> are generated by the same <code>release-notes</code> subcommand (<code>dev/build/kirivers_build/notes.py</code>), in the format <code>scripts/sdk_release.py</code> uses on the SDK branches:</p><div class="language-markdown line-numbers-mode"><button title="Copy code" data-copied="Copied" class="copy"></button><span class="lang">markdown</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#005CC5;--shiki-light-font-weight:bold;--shiki-dark:#79B8FF;--shiki-dark-font-weight:bold;">## v0.3.0 — 2026-10-05</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-light-font-weight:bold;--shiki-dark:#79B8FF;--shiki-dark-font-weight:bold;">### 💥 Breaking Changes</span></span>
<span class="line"><span style="--shiki-light:#c13617;--shiki-dark:#FFAB70;">-</span><span style="--shiki-light:#24292E;--shiki-light-font-weight:bold;--shiki-dark:#E1E4E8;--shiki-dark-font-weight:bold;"> **admin**</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> feat: 重写 API Token 轮换，旧接口下线 (8a9b0c1) #31</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-light-font-weight:bold;--shiki-dark:#79B8FF;--shiki-dark-font-weight:bold;">### ✨ Features</span></span>
<span class="line"><span style="--shiki-light:#c13617;--shiki-dark:#FFAB70;">-</span><span style="--shiki-light:#24292E;--shiki-light-font-weight:bold;--shiki-dark:#E1E4E8;--shiki-dark-font-weight:bold;"> **delta**</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: 支持流式源文件 (4f5e6d7) #28</span></span>
<span class="line"><span style="--shiki-light:#c13617;--shiki-dark:#FFAB70;">-</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> 直推且没有 scope 的 feat 照进本节 (6bc7e20)</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-light-font-weight:bold;--shiki-dark:#79B8FF;--shiki-dark-font-weight:bold;">### 🐛 Bug Fixes</span></span>
<span class="line"><span style="--shiki-light:#c13617;--shiki-dark:#FFAB70;">-</span><span style="--shiki-light:#24292E;--shiki-light-font-weight:bold;--shiki-dark:#E1E4E8;--shiki-dark-font-weight:bold;"> **delta**</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: 拒绝截断的 HDIFF13 头部 (a1b2c3d) #29</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br><span class="line-number">9</span><br><span class="line-number">10</span><br><span class="line-number">11</span><br></div></div><ul><li>A section is a commit <code>type</code>; its heading is <code>### &lt;emoji&gt; &lt;label&gt;</code>. The emoji, the label and the <strong>section order</strong> all come from the type table — file order is section order.</li><li>An entry is <code>- **&lt;scope&gt;**: &lt;description&gt; (&lt;sha7&gt;) #&lt;PR&gt;</code>. The <code>scope</code> is lowercased and stripped of whitespace (<code>perf(DB)</code> and <code>perf(db)</code> are the same label). With no scope, the <code>**scope**:</code> prefix is omitted; the entry is not dumped into any catch-all section.</li><li>Breaking entries (<code>type!:</code> or a body <code>BREAKING CHANGE:</code> / <code>BREAKING-CHANGE:</code>) leave their own type section, gather in the first section <code>### 💥</code>, and get the type word restored inline — because the section heading no longer carries it.</li><li>The PR number is <strong>decoration</strong>: the job queries <code>gh api .../commits/&lt;sha&gt;/pulls</code> and silently omits it when the lookup fails, <code>gh</code> is missing or the network is down. It never blocks a release. A subject that already ends in <code>(#123)</code> does not get a second number.</li><li>Subjects that are not Conventional Commits do not appear (the same set that &quot;does not release&quot;). A valid but unregistered type lands in <code>### 🔖 Other</code>, so <strong>entries are never dropped silently</strong>. When the range holds no displayable entry at all, the output is <code>No user-facing Conventional Commits in this range.</code></li><li>Without a previous <code>v*</code> tag the output is the baseline statement only: no history replay, no sections.</li></ul><p>Two runs over the same range produce byte-identical output.</p><h3 id="custom-emoji-labels-hiding-a-type" tabindex="-1">Custom emoji / labels / hiding a type <a class="header-anchor" href="#custom-emoji-labels-hiding-a-type" aria-label="Permalink to “Custom emoji / labels / hiding a type”">​</a></h3><p>The only entry point is <code>dev/build/changelog-types.conf</code> (<code>scripts/changelog-types.conf</code> on the SDK branches, byte-identical across all twelve copies):</p><div class="language-ini line-numbers-mode"><button title="Copy code" data-copied="Copied" class="copy"></button><span class="lang">ini</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">breaking</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">=💥=Breaking Changes</span></span>
<span class="line"><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">feat</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">=✨=Features</span></span>
<span class="line"><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">fix</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">=🐛=Bug Fixes</span></span>
<span class="line"><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">perf</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">=⚡=Performance</span></span>
<span class="line"><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">revert</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">=⏪=Reverts</span></span>
<span class="line"><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">refactor</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">=♻️=Refactoring</span></span>
<span class="line"><span style="--shiki-light:#62687b;--shiki-dark:#818e99;"># docs=📝=Documentation</span></span>
<span class="line"><span style="--shiki-light:#62687b;--shiki-dark:#818e99;"># chore=🔧=Chores</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br></div></div><p>One entry per line, <code>type=emoji[=section label]</code> (<code>type=emoji label</code> is accepted as well). A full-line comment of the form <code># type=…</code> means <strong>that type stays out of the changelog</strong>; every other <code>#</code> line is prose. Malformed input — a key that does not start with a lowercase letter, a missing emoji, the same type listed twice — exits with an error instead of being ignored. When the file is missing, the script falls back to its built-in default table and asserts that &quot;the parsed result equals the file&quot;, so the two copies of the defaults cannot drift.</p><div class="danger custom-block"><p class="custom-block-title">Hold this invariant</p><p>The type table is <strong>a display layer only</strong>. The bump table in <code>next-version</code> and <code>RELEASE_TYPES</code> in <code>sdk_release.py</code> never read it — commenting out <code>fix=</code> only removes the Bug Fixes section; the patch bump still happens. The regression assertion lives in <code>release-notes --self-check</code>: <code>next-version --format env</code> must print byte-identical output before and after a config change.</p></div><h2 id="changelog-md-is-written-back-to-main" tabindex="-1">CHANGELOG.md is written back to main <a class="header-anchor" href="#changelog-md-is-written-back-to-main" aria-label="Permalink to “CHANGELOG.md is written back to main”">​</a></h2><p>After creating the tag and the Release, the <code>github-release</code> job prepends the header section to <code>CHANGELOG.md</code> and runs <code>git push origin HEAD:main</code> with the commit message <code>docs(release): v&lt;semver&gt; changelog</code> (<code>docs:</code> does not count toward the next release, so it cannot invent a version delta). That commit is therefore not part of the released commit set and is not an ancestor of the <code>&lt;semver&gt;</code> tag.</p><p>This requires the repository to let <code>github-actions[bot]</code> write <code>main</code> with <code>GITHUB_TOKEN</code> (the same shape as allowing a bot to merge on the SDK branches). If branch protection does not permit it, the step <strong>fails and states why</strong>; it never skips silently. The SDK branches behave the same way, and <code>prepend_changelog</code> creates <code># Changelog</code> when the file is absent.</p><h2 id="docker-native-build-per-architecture-manifest-composed-remotely" tabindex="-1">Docker: native build per architecture, manifest composed remotely <a class="header-anchor" href="#docker-native-build-per-architecture-manifest-composed-remotely" aria-label="Permalink to “Docker: native build per architecture, manifest composed remotely”">​</a></h2><table tabindex="0"><thead><tr><th>Tag</th><th>Meaning</th></tr></thead><tbody><tr><td><code>kirizuofficial/kirivers:latest</code></td><td>points at the current release</td></tr><tr><td><code>kirizuofficial/kirivers:&lt;semver&gt;</code></td><td>the release&#39;s multi-arch manifest</td></tr><tr><td><code>kirizuofficial/kirivers:&lt;semver&gt;_amd64</code></td><td>that version&#39;s single-architecture amd64 image</td></tr><tr><td><code>kirizuofficial/kirivers:&lt;semver&gt;_arm64</code></td><td>that version&#39;s single-architecture arm64 image</td></tr></tbody></table><p>Each of the two architectures builds on a <strong>native runner</strong> with <code>docker/login-action@v3</code> + <code>docker/build-push-action@v6</code> (<code>--build-arg USE_PREBUILT=1</code> reuses the already-built musl binary) and pushes its per-architecture tag; the <code>docker-manifest</code> job then composes <code>:latest</code> and <code>:&lt;semver&gt;</code> registry-side with <code>buildx imagetools create</code>. <code>imagetools create</code> executes nothing inside the image, so no QEMU or binfmt is needed. glibc artifacts <strong>never</strong> enter the Alpine image: each per-architecture job COPYs only a <code>linux-musl</code> matrix binary (musl assertion passed); the local <code>docker-image</code> path likewise accepts only binaries that passed the musl assertion and requires the host architecture to match the target.</p><p>Job ordering guarantees the manifest is composed only once both per-architecture tags have been pushed — composing the wrong tag would poison <code>:latest</code>.</p><h2 id="secrets-and-disabling-publish" tabindex="-1">Secrets and disabling publish <a class="header-anchor" href="#secrets-and-disabling-publish" aria-label="Permalink to “Secrets and disabling publish”">​</a></h2><p>Document names only, never values:</p><table tabindex="0"><thead><tr><th>Name</th><th>Use</th></tr></thead><tbody><tr><td><code>DOCKERHUB_USERNAME</code></td><td>Docker Hub user</td></tr><tr><td><code>DOCKERHUB_TOKEN</code></td><td>Docker Hub login</td></tr><tr><td><code>SERVER_PUBLISH</code></td><td>GitHub repository variable; with <code>false</code> the run only derives the version</td></tr><tr><td><code>GITHUB_TOKEN</code></td><td>provided by the workflow; writes the tag, the Release and <code>CHANGELOG.md</code>, reads PR numbers</td></tr></tbody></table><p>Missing Hub credentials fail the <strong>image push</strong> step and name <code>DOCKERHUB_USERNAME</code> and/or <code>DOCKERHUB_TOKEN</code>; logs must not print the token. Concurrency group: <code>server-release</code>.</p><h2 id="local-image-build" tabindex="-1">Local image build <a class="header-anchor" href="#local-image-build" aria-label="Permalink to “Local image build”">​</a></h2><div class="language-bash line-numbers-mode"><button title="Copy code" data-copied="Copied" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">docker</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> build</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --platform</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> linux/amd64</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -t</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> kirivers:linux-amd64</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -f</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> dev/build/Dockerfile</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> .</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>For day-to-day runs use the Hub image from the operator one-click pack <code>deploy/</code> (<code>image: kirizuofficial/kirivers:latest</code>, see <a href="/en/guide/install/compose">Docker Compose</a>). A local <code>docker build</code> is only for iterating on the Dockerfile before pushing to Hub. The image defaults <code>KIRIVERS_STORAGE_LOCAL_ROOT=/data/storage</code> and the log directory to <code>/data/logs</code>. Bind-mount a host directory at <code>/config</code> and <code>/data</code> to persist the YAML, the full and delta objects, and the logs.</p><p>HEALTHCHECK hits the admin plane <code>GET /api/v1/health</code> (HTTP 200 even without a database). The container runs as root so bind mounts keep simple ownership.</p><h2 id="must-test-items" tabindex="-1">Must-test items <a class="header-anchor" href="#must-test-items" aria-label="Permalink to “Must-test items”">​</a></h2><p>After touching this chain, run at least:</p><div class="language-bash line-numbers-mode"><button title="Copy code" data-copied="Copied" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">python3</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> dev/build/kirivers.py</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> check-workflows</span><span style="--shiki-light:#62687b;--shiki-dark:#818e99;">        # static gate: triggers, matrix, zero QEMU, no second-forge residue</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">python3</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> dev/build/kirivers.py</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> release-notes</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --self-check</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">python3</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> dev/build/kirivers.py</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> guard</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --self-check</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">python3</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> dev/build/kirivers.py</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> issue-link</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --self-check</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">CGO_ENABLED</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">1</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> go</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> build</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> .</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> &amp;&amp; CGO_ENABLED</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">0</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> go</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> build</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> .</span><span style="--shiki-light:#62687b;--shiki-dark:#818e99;">   # the second must fail</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br></div></div><p>On Windows replace <code>python3</code> with <code>python</code>. <code>check-workflows</code> already encodes assertions such as &quot;only <code>workflow_dispatch</code>&quot;, &quot;no <code>setup-qemu-action</code>&quot; and &quot;<code>pr-guard.yml</code> never checks out PR code&quot;, and the <code>changelog</code> job in CI runs it.</p>`,52)]])}var s=r(a,[[`render`,o]]);export{i as __pageData,s as default};